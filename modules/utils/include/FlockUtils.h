#pragma once

/**
 * FlockUtils Module - Utility classes and helpers
 * 
 * This module provides utility classes and helper functions that can be
 * used across different projects.
 * 
 * Components:
 * - PerformanceMonitor: Performance timing and monitoring utilities
 * - Random: Random number generation utilities
 */

// Utility classes
#include "PerformanceMonitor.h"
#include "../../../modules/math/include/MathUtils.h"  // Random utilities now in math module

namespace flock {
namespace utils {
    // Convenience functions can be added here
}
}
