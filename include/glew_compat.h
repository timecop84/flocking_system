#ifndef GLEW_COMPAT_H
#define GLEW_COMPAT_H

// Simple GLEW compatibility stub
// For a full implementation, link against actual GLEW library
inline int glewInit() {
    // Stub implementation - in a real app, link against GLEW
    return 0; // GLEW_OK
}

#endif // GLEW_COMPAT_H
